READ ME

To run the app:

```
jekyll serve
```

Stash local changes:
```
git stash
```

Pull changes:
```
git pull --rebase
```

To commit changes:
```
git add .
git commit -m "Commit message here"
git push
```
